# URL del backend para el build de PRODUCCION en Vercel.
# Apunta al tunel Cloudflare que expone el backend local (puerto 3000).
# NOTA: la URL de un "quick tunnel" trycloudflare cambia cada vez que se
# reinicia el contenedor cloudflared; si dejas de probar, vuelve a generarla
# y actualiza este valor (luego redeploy en Vercel).
VITE_API_URL=https://cordless-carl-lover-liquid.trycloudflare.com
